// Million.js configuration for ESM
export default {
  telemetry: false, // Disable telemetry to prevent fetch errors
  enabled: true,
  rsc: true,
  mode: 'legacy',
  server: false, // Disable the connection to dev server
};
